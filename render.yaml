databases:
  - name: facerecog-db
    plan: free

services:
  - type: web
    name: facerecog-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: TASK_TOKEN
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: facerecog-db
          property: connectionString

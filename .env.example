# Local secrets for development only.
# Do not commit a real .env file.

SECRET_KEY=replace-with-a-random-secret
TASK_TOKEN=replace-with-a-random-token
NEON_DATABASE_URL=postgresql://username:password@your-neon-host/neondb?sslmode=require&channel_binding=require
